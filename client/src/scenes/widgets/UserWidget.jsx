import React from "react";

const UserWidget = ({ userId, picturePath }) => {
  return (
    <div>
      <p>UserWidget Placeholder</p>
      <p>User ID: {userId}</p>
      <p>Picture Path: {picturePath}</p>
    </div>
  );
};

export default UserWidget;
